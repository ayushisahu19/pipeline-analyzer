version: "3.8"

services:

  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  api:
    build: .
    container_name: pipeline-analyzer-api
    ports:
      - "5000:5000"
    depends_on:
      - mongodb